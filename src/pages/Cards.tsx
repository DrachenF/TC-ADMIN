import CardTile from '../components/CardTile';import { useApp } from '../hooks/useAppState';export default function Cards(){const {state}=useApp();return <><div className="row between"><h1>Tarjetas</h1><a className="primary" href="/cards/new">+ Agregar tarjeta</a></div>{state.cards.length===0?<section className="empty"><h2>Agrega tu primera tarjeta</h2><p>Controla tus gastos, pagos y fechas desde un solo lugar.</p><a className="primary" href="/cards/new">+ Agregar tarjeta</a></section>:<div className="cards">{state.cards.map(c=><CardTile card={c} key={c.id}/>)}</div>}</>}
